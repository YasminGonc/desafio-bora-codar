import { styled } from '../../styles'

export const LayoutContainer = styled('main', {
  // backgroundColor: 'red',
  width: '100vw',
  height: '100vh',
  display: 'grid',
  gridTemplateColumns: '75% 25%',
})
